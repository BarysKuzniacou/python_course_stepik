# Продолжите программу, в которой формируются две переменные x, y следующей командой:

# x, y = map(int, input().split())

                  
# Используя операции /= и *= , уменьшите значение переменной x 
# в 2 раза и увеличьте значение переменной y в 3.5 раза.

# P.S. На экран ничего выводить не нужно.

x, y = map(int, input().split())

x /= 2
y *= 3.5