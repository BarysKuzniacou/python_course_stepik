# [ok] lambda x: x

# [ok] lambda x, y: x+y

# [ok] lambda a: -a

# [ok] lambda: "hello lambda"

# lambda x: return x

# lambda: return "hello lambda"

# lambda a, b: a, b = b, a

# lambda d: d += 1

# lambda d: (d += 1)